tasks.withType(Test).configureEach {
    useJUnitPlatform()
    testLogging {
        events 'passed', 'skipped', 'failed', 'standardOut', 'standardError'
    }
    systemProperty('grails.env', System.getProperty('grails.env', 'test'))
}
